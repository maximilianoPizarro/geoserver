#!/bin/bash 

# Run additional build before steps

echo "custom mvn src"

cd /tmp/src/src

#!/bin/bash 

# Run additional build before steps

echo "custom mvn src"

cd /tmp/src/src

mvn -e -DskipTests -Dfabric8.skip=true --batch-mode -Djava.net.preferIPv4Stack=true -s /home/jboss/.m2/settings.xml -Dmaven.repo.local=/tmp/artifacts/m2 package
