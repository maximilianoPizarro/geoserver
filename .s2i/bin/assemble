#!/bin/bash 

# Run additional build
echo "...maven install"
echo "mvn clean install --batch-mode -DskipTests -P allExtensions,release"

cd /tmp/src/src

mvn clean install --batch-mode -DskipTests -P allExtensions,release
