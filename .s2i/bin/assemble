#!/bin/bash 

# Run additional build before steps

echo "custom mvn src"

cd /tmp/src/src

#!/bin/bash 

# Run additional build before steps

echo "custom mvn src"

cd /tmp/src/src

mvn clean install -DskipTests -P allExtensions,release
